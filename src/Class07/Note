NestedLoops:
   A loop inside another loop. just like nested if statements.

Why we do we need them?
   to solve complex problem that requires accessing nested data.

Array:
    array is a data structures that helps us store multiple items in a single variable. we can
    use the indexes to access these values.
