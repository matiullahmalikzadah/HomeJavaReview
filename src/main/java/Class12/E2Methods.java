package Class12;

public class E2Methods {

    // here we use the data type (int) repalcement of void due we
    //want to store the
    // in this time we have to use return also
    int add(int num1,int num2){
        int result=num1+num2;
        return result;
    }
    // create a method which take a number multiply it with 10
    // and returns the result

    int multiply (int num){
        int res=num*10;
         return res;
    }

}
